# Epic: Implementation logs — keep the history, not just the distillate

Status: complete
Approved: 2026-08-27
Completed: 2026-08-27 (units 1-5, v1.76.0-v1.79.0; unit 5 shipped as a docs-only commit
with no bump)

## Goal

`PLAN.md` and `EPIC.md` are deleted at cleanup today, so the only thing that survives a task
or an epic is whatever got distilled into a `knowledge/` concept — the invariant, never the
reasoning that produced it. Keep both as an append-only implementation log inside the
knowledge bundle. Done when a finished task and a finished epic each leave a durable,
validator-clean log entry, in a repo with a bundle and in a repo without one.

Provenance: a BigIn engineer's suggestion that keeping the history beats distilling and
tracking only the current state. It reverses a position `docs/KNOWLEDGE.md` states explicitly
(§6, "Per-task narrative in `knowledge/`" as a non-goal); unit 5 rewrites that into a
boundary rather than leaving the two in contradiction.

## Constraints

- **Each unit ships itself** — its own `CHANGELOG.md` entry and a version bump across the four
  manifest version fields (`.claude-plugin/plugin.json` is the source of truth;
  `.cursor-plugin/plugin.json` and two fields in `.cursor-plugin/marketplace.json` follow).
  `.claude-plugin/marketplace.json` carries no version field.
- **No OKF v0.2 divergence.** `RESERVED` stays `{index.md, log.md}` — those two are reserved by
  OKF §8-§9 and adding a third is a format fork. Log entries are ordinary concept files under a
  new allowed `type`, so the validator's shape is unchanged.
- **`knowledge/implementation/`, never `knowledge/logs/`.** `knowledge/log.md` already exists as
  OKF's per-sprint change history; two homes one character apart will be confused.
- **The log must not reach the always-loaded surface.** The index-first protocol reads
  `knowledge/index.md`; implementation logs get their own nested index and are not read
  preemptively. Budget gate at epic start: 8267/12000 chars, and it should not move.
- **Units 1 and 5 touch `bigin-harness-setup/references/knowledge-bundle.md`**, which is copied
  verbatim into target repos — each needs a CHANGELOG `patch` block or already-scaffolded repos
  never receive the change.
- **The no-bundle fallback is not optional.** Neither this repo nor most target repos have a
  `knowledge/` bundle. A bundle-only log means "never delete again" silently fails in the
  majority of repos, which is the whole point of the epic.

## Units

| # | Unit | Acceptance | Blocked by | Status | Notes |
|---|------|-----------|------------|--------|-------|
| 1 | Bundle format gains `knowledge/implementation/` and `type: implementation-log` | `knowledge-bundle.md`'s spec, frontmatter schema, allowed-`type` list, validator and templates all accept a log entry; a nested `knowledge/implementation/index.md` keeps entries index-reachable so the validator raises no unreachable warning; `RESERVED` is unchanged; a CHANGELOG `patch` block delivers it to already-scaffolded repos | — | Done | Shipped as **v1.76.0**. **Type is `Record`, not `implementation-log`** — `ALLOWED_TYPES` is capitalized single nouns (`Contract`, `Playbook`, `Constraint`) and `Log` is already a v0.1 `LEGACY_TYPES` entry the validator warns on, so the approved name would have shipped inconsistent and half-colliding. Folder is `implementation/` as approved. **The only code change is one entry in `ALLOWED_TYPES`** — everything else the format already supported, which is why there is no OKF divergence: `RESERVED` is untouched, records are ordinary concept files, and nested `index.md` files were already reachability *seeds* rather than leaves. That last property is load-bearing for the whole epic — appending a record touches `knowledge/implementation/index.md` and never the bundle-root index the index-first protocol reads, so a bundle with two hundred records costs the same per-session context as one with none. **A defect caught before shipping, twice, both the same species:** the root-index template links `/implementation/index.md`, so that file had to join Phase 5.5 step 2's canonical starter list in `bigin-harness-setup/SKILL.md` or every fresh scaffold would fail its own validator on a broken link; and the nested-index template originally carried an example record bullet, which would have been a second broken link — the template now says explicitly not to seed one. `knowledge-distill` Phase 0a reuses that starter list verbatim, so it follows automatically. Verified against an extracted copy of the patched validator on a real fixture, four cases: a `Record` linked from the nested index gives 0 errors 0 warnings; the same fixture on the pre-change validator errors `type 'Record' not in allowed list` (so the one line is what enables it, nothing else); an unlinked record warns `not reachable from an index.md` (so units 2-3 appending to the index is enforced, not merely instructed); and a fresh scaffold — empty index, zero records — is clean. Exit 1 on error, 0 on clean, so the pre-commit gate holds. Six CHANGELOG `patch` blocks (three spec anchors, the validator line, the root index, plus `create-if-missing` for the nested index); patch mode applies every block in an entry in order, so one entry carries all six. This repo has no `knowledge/` bundle, so the fixture in the session scratchpad is the only dogfooding available — kept out of the repo. Implement/verify loop not run: session is configured not to call the Agent tool unless asked. **For units 2-4:** the record body is the working file *verbatim*, not a summary — that is the point of the epic, so do not let a "concise" record slip in; and `Record` is exempt from the entire staleness policy, which unit 4 has to respect in `sprint-distill`'s sweep.
| 2 | `task-workflow` step 6 archives `PLAN.md` instead of deleting it | A finished task writes a validator-clean entry under `knowledge/implementation/` and `PLAN.md` is gone from the repo root; with no bundle present it writes `.claude/memory/PLAN.archive.<ISO>-<slug>.md` instead; the distill prompt is unchanged and the log entry does not restate the concept it proposed | 1 | Done | Shipped as **v1.77.0**. Step 6's lead verb is now *archive*, with the two destinations as a `never both` pair after the distill and graph bullets — bundle path writes `{YYYY-MM-DD}-{slug}.md` + one nested-index line, no-bundle path writes `.claude/memory/PLAN.archive.{ISO}-{slug}.md`, and creating `knowledge/implementation/` to have a home for one record is explicitly ruled out. **Unit 3 should mirror this structure verbatim for `EPIC.md`** — same two destinations, same `source:` field (`epic` not `plan`), same verbatim rule — so the two skills don't drift into two dialects of one convention. **The distill prompt is unchanged in substance but one clause had to move:** it opened with "deleting it is the last chance to keep any of that", which is false once the plan is archived; it now leads with the narrative/invariant boundary instead, and the record-vs-concept paragraph at the end of the step states that boundary explicitly. Expect the same false-premise clause in `epic-workflow` step 10 and check for it. **This unit dogfooded its own no-bundle path** — this repo has no `knowledge/`, so cleanup archived the plan to `.claude/memory/PLAN.archive.20260827T000000Z-task-workflow-archives-plan-md.md`; the bundle path is still unexercised anywhere, which makes unit 5's docs the only place a reader will see it described. Scope grew by one file over the approved spec, with sign-off: `files-shared.md`'s `AI_TASK_GUIDE.md` template line is copied verbatim into target repos, so it shipped with a CHANGELOG `patch` block rather than waiting for unit 5 — **unit 5 must not re-fix that line**. Two more stale cross-references corrected in the same pass (`epic-workflow` step 7, `discovery-workflow`'s lifetime table); the ones left for unit 5 are `docs/KNOWLEDGE.md` §6/§3/§5 + line 93/96/155, `docs/SPEC-GATE.md:146`, and `docs/USER_GUIDE.md:176/251`. Budget gate 8267 chars, unmoved. Implement/verify loop not run: session is configured not to call the Agent tool unless asked. |
| 3 | `epic-workflow` step 10 archives `EPIC.md` instead of deleting it | Same two paths as unit 2, for `EPIC.md`, including the `## Amendments` log; the epic-level distill prompt is unchanged | 1 | Done | Shipped as **v1.78.0**. Step 10 mirrors `task-workflow` step 6 structurally — lead verb *archive*, the two prior bullets (distill, graph) unchanged, then a `never both` destination pair, then the verbatim rule and the record-vs-concept boundary — so the two skills read as one convention rather than two dialects. Differences are only the ones the epic layer forces: `source: epic`, `shipped:` is a **list** of versions (one per unit, not one), and the verbatim body adds `## Not in scope` alongside goal/constraints/unit-table/amendments. **The false-premise clause unit 2 warned to look for was there, in a different form.** Not in the distill bullet (step 10's already led with "expect to find something" and already read `## Amendments` first, so it needed no substantive change) but in the delete bullet itself: "nothing to preserve once the last unit ships and the distillation is in `knowledge/`" — the exact claim units 1 and 2 disproved. It's gone rather than reworded. **One cross-reference correction was larger than a phrase swap:** `discovery-workflow`'s lifetime table closes with "only the first survives, which is why it is the only one of the three that is worth getting the format of exactly right" — true of all three files when written, now true of none but the PRD. Rewritten to say the PRD is the only one that stays a *live document* while the other two survive as records, which keeps the original point (the PRD's format is the one worth getting right) on a premise that still holds. Unit 5 should expect this species of staleness in `docs/KNOWLEDGE.md` too: sentences whose *conclusion* is still correct resting on a "these get deleted" premise that isn't. Also fixed: `epic-queue.md`'s "deleted at step 10". `knowledge-bundle.md:158` ("about to be removed") was checked and left — the working file *is* still removed, after the record is written. Budget gate 8267 chars, unmoved; `docs_sync --check` clean. No CHANGELOG `patch` block needed: nothing this unit touched is copied verbatim into target repos (`epic-workflow/` and `discovery-workflow/` are plugin skills, and their references aren't under `bigin-harness-setup/`). Bundle path still unexercised — this repo has no `knowledge/`, so like unit 2 only the no-bundle path is dogfooded, and unit 5's docs remain the only place a reader will see the bundle path described. Implement/verify loop not run: session is configured not to call the Agent tool unless asked. |
| 4 | `sprint-distill` exempts the log from its compression sweep | Its stale-concept sweep never proposes deleting or shrinking a `type: implementation-log` file, and says why in place; its per-sprint `knowledge/log.md` entry may cite implementation logs but never absorbs them | 1 | Done | Shipped as **v1.79.0**. Read `type: Record` for `type: implementation-log` throughout — unit 1's rename, already recorded there; the acceptance cell is left at the approved wording rather than rewritten. **The sweep needed three exemptions, not one**, because each of Phase 2's checks fails on a record for a different reason, and only stating all three keeps a later reader from re-adding the one they don't see refuted: the citation sweep reads a moved target as staleness when a record cites the code *as it stood*; the reachability sweep looked for a bundle-root index entry that records deliberately don't have; and the graph-overlap sweep's remedy is "compress or merge", which isn't available for a file that is never edited after it's written. Stated once at the head of the stale-concept sweep, and scoped to cover the net-neutral rule and the ~60-line concept ceiling too — a verbatim plan clears 60 lines routinely, so that ceiling alone would have forced a shrink proposal. **Two places needed more than an exemption.** Phase 1's graph symbol-miss scan now skips `implementation/` before building the list, rather than emitting flags Phase 2 discards — the flags are what a reader acts on, so filtering late is not equivalent. And classification gained a fourth sorting rule beside *never both*: **never a record**, because the folder is write-only from this skill's side (only `task-workflow` and `epic-workflow` cleanup write it) and a record is never opened to be mined — it is what distillation already ran against. Without that rule the WHAT/WHY branch would happily route a record's narrative into a concept file, which is the same double-write the epic exists to prevent. One stale claim fixed in passing, the species unit 3 flagged: the reachability bullet credited `knowledge_validate.mjs` with seeding reachability from `knowledge/index.md` alone, when it seeds from **every** `index.md` — the exact property unit 1 relies on. No CHANGELOG `patch` block: `skills/sprint-distill/` is a plugin skill, nothing under `bigin-harness-setup/references/`, so no target repo holds a copy. Evals are trigger-only and unchanged. Budget gate 8267 chars, unmoved; `docs_sync --check` clean. Spec gate skipped (prose-only change to one file, well under 20 lines of logic) — same as unit 3, so no `PLAN.md` and no record to archive. Implement/verify loop not run: session is configured not to call the Agent tool unless asked. **For unit 5:** `sprint-distill` is now consistent with the record convention, so the docs sweep doesn't need to revisit it — but `docs/KNOWLEDGE.md` §6's non-goal is the *source* of the position this whole epic reversed, and the log-vs-`log.md` distinction (one-entry-deep index vs. the narrative itself) is now stated in `sprint-distill` Phase 4 and should not be restated in the docs, only linked. |
| 5 | Docs reconciliation | `docs/KNOWLEDGE.md` §6's "per-task narrative" non-goal is rewritten as a boundary (narrative → the log, invariant → a concept, neither restating the other), §3 and §5's mermaid cover the new home, and `docs/USER_GUIDE.md` + `README.md` describe cleanup as archiving rather than deleting | 2, 3, 4 | Done | Shipped as a **`docs:` commit with no version bump**, with sign-off: the epic's per-unit bump constraint yields to `CLAUDE.md`'s docs-only exemption, because nothing under `docs/` is copied into a target repo — which also means no CHANGELOG `patch` block. So this epic's record lists four versions for five units. **§6's non-goal became a boundary without being promoted out of the "four mistakes" list**, because the mistake is still real — it only moved: not "per-task narrative in `knowledge/`" but "per-task narrative in a *concept* file", with the narrative's actual home named beside it. Same restraint in §3: records are a **third population on a different axis** rather than a rename to "Three kinds of knowledge", since that section's axis is ours-vs-third-party and records cut across it. **The staleness species unit 3 predicted was there, in the one sentence hardest to spot:** §6's closing line, that a graph, a rule file and a `PLAN.md` together still can't say *why* the retry logic sits where it does. Conclusion still right, premise now false — a record *does* say why. Repaired by adding **today**: a record says why one change was made, not what is now true, which is the concept/record boundary restated at the section's own altitude rather than a hedge. **§5's mermaid draws the exemption instead of only asserting it:** records enter as dashed edges from the two cleanup nodes into their own store, which flows to `knowledge_validate.mjs` and *not* to the stale sweep — so a reader sees which of the four honesty mechanisms applies to a record without reading the paragraph. The four flagged lines are fixed as flagged (`SPEC-GATE.md:138/146`, `USER_GUIDE.md:176/251`), plus the surface table five → six rows and `PLAN.md`'s lifetime "Deleted at task end" → "Archived verbatim at task end". `USER_GUIDE.md` §8 gained one paragraph — the only user-facing place a reader learns records exist, so it carries the no-bundle path too. **`README.md` needed no edit and got none:** it never described cleanup's file handling, so there was nothing stale to fix, and inventing a mention to satisfy the acceptance cell would have cost front-door prose for a detail the guide already covers. Per unit 4's note, `sprint-distill` was not revisited and the log-vs-`log.md` distinction is linked, not restated; `files-shared.md`'s `AI_TASK_GUIDE.md` line was left alone, as unit 2 required. Budget gate 8267 chars, unmoved; `docs_sync --check` clean. Bundle path still unexercised anywhere — this repo has no `knowledge/`, so every description of it in these docs is written from the spec, not from a run. Implement/verify loop not run: session is configured not to call the Agent tool unless asked. |

## Not in scope

- Backfilling logs for tasks and epics already shipped. The two existing
  `.claude/memory/*.archive.*.md` files stay where they are.
- Changing what gets distilled into a concept. The distill prompts in `task-workflow` step 6 and
  `epic-workflow` step 10 are untouched — this epic adds a second destination, it does not
  redirect the first.
- A retention or pruning policy for the log. Append-only is the point; if the bundle later needs
  one, that is its own decision.
