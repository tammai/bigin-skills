# STUB — replace with the real backend contract.
openapi: 3.1.0
info:
  title: {PROJECT_NAME} backend API
  version: 0.0.0
paths:
  /users:
    get:
      responses:
        '200':
          description: user list
